{{define "content"}}
{{$p := .Item}}
<section class="cmn-page-banner">
    <div class="cmn-container">
        <nav class="cmn-breadcrumb" aria-label="Breadcrumb">
            <a href="{{langPrefixURL .Ctx "/"}}">{{T .Ctx "nav_home"}}</a><span aria-hidden="true">/</span><span>{{$p.Title}}</span>
        </nav>
        <h1>{{$p.Title}}</h1>
        {{if $p.Excerpt}}<p class="cmn-page-banner-sub">{{$p.Excerpt}}</p>{{end}}
    </div>
</section>
<section class="cmn-section">
    <div class="cmn-container">
        {{if $p.ImageURL}}<figure class="cmn-page-hero cmn-page-hero-wide">{{responsiveImage $p.ImageURL $p.Title "" "100vw" "lazy"}}</figure>{{end}}
        <div class="cmn-richtext cmn-richtext-wide">{{safeHTML $p.Content}}</div>
    </div>
</section>
{{end}}
