{{define "content"}}
<section class="ce-detail-hero">
    <span>Service</span>
    <h1>{{.Service.Title}}</h1>
    <p>{{.Service.Excerpt}}</p>
</section>

<section class="ce-detail-layout">
    <article class="ce-detail-card">
        {{if .Service.ImageURL}}<div class="ce-detail-media">{{responsiveImagePriority .Service.ImageURL .Service.Title "" "(max-width: 768px) 100vw, 62vw"}}</div>{{end}}
        <div class="ce-richtext">{{safeHTML .Service.Description}}</div>
    </article>
    <aside class="ce-detail-aside">
        <h3>More services</h3>
        {{range .Related}}<a href="{{langPrefixURL $.Ctx (printf "/services/%s" .Slug)}}">{{.Title}} <span>→</span></a>{{end}}
    </aside>
</section>
{{end}}
