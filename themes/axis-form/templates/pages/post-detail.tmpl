{{define "content"}}
<section class="af-detail-hero">
    <span>Journal</span>
    <h1>{{.Post.Title}}</h1>
    <p>{{formatDateTime .Post.PublishedAt}}</p>
</section>

<section class="af-detail-layout">
    <article class="af-detail-card">
        {{if .Post.ImageURL}}<div class="af-detail-media">{{responsiveImagePriority .Post.ImageURL .Post.Title "" "(max-width: 768px) 100vw, 62vw"}}</div>{{end}}
        <div class="af-richtext">{{safeHTML .Post.Content}}</div>
    </article>
    <aside class="af-detail-aside">
        <h3>Related articles</h3>
        {{range .Related}}<a href="{{langPrefixURL $.Ctx (printf "/blog/%s" .Slug)}}">{{.Title}} <span>→</span></a>{{end}}
    </aside>
</section>
{{end}}
